# Delta-review correctieronde 3 (F1 + F9) — DEF-766 ESS-03

**Beoordeelde staat:** HEAD `0ec86aced` + ongecommitte wijzigingen (7 bron- + 1 UI-bestand, 3 nieuwe testbestanden; `git diff --stat`: 8 bestanden, +217/−40). Alle 10 uitvoerdersbestanden zijn byte-gelijk aan `correction-manifest-v3.json` (zelf gehasht); `definition_edit_tab.py` bevat alleen de coördinatorguard (regels 1324–1327). Read-only, geen modelcalls, geen agents.

## Bevindingen

| Id | Ernst | Vindplaats | Bevinding | Bewijs | Dispositie |
|---|---|---|---|---|---|
| **D1** | opmerking (verdedigbaar) | `config/config.yaml:245-263`; `test_anthropic_client_thinking_guard.py:95-96,180-189` | Fable 5/Mythos 5 bewust **niet** in `thinking_default_on`: de uitvoerder volgde zijn referentie ("`disabled` → 400 op Fable/Mythos"); de door de coördinator geverifieerde documentatie zegt dat `disabled` bij effort ≤ high is toegestaan. De bronnen spreken elkaar tegen; de uitvoerder koos de fail-safe kant (weglaten is altijd geldig). De app routeert niet naar die modellen; het mechanisme volgt élke geconfigureerde familie (test met geïnjecteerde router `["opus-5","fable-5"]` → `disabled`). | tests groen; configcommentaar benoemt de bron. | **waiver met reden**: verdedigbaar zolang Fable/Mythos niet gerouteerd worden; bij een toekomstige routewissel eerst de bron beslechten en één configregel toevoegen. Configcommentaar zou het bronconflict expliciet mogen noemen. |
| **D2** | opmerking | `model_router.py:197-214` | Familie-matching met numerieke grens: `claude-opus-50` → omit (getest), maar een hypothetisch `claude-opus-5-1` matcht `opus-5` (volgend teken `-`) → `disabled`. Voor de Opus-5-familie is dat volgens de coördinatordocumentatie geldig; het is dezelfde grensregel als de temperature-guard (DEF-441). | eigen redenering op de code; niet getest. | opmerking — geen actie. |
| **D3** | opmerking | `anthropic_client.py:176`; alle Anthropic-routes | Regressierisico andere routes: elke Opus-5-aanroep (generatie 300/500/800 tokens, synoniemen 800, bronvoorstel) krijgt nu `thinking: disabled`. Dat is het dichtst bij het gedrag van vóór de modelwissel (Opus 4.8 zonder denken) en beschermt de kleine `max_tokens`-budgetten; zónder de guard zouden die routes door denktokens afkappen. Restrisico (modelreferentie): met denken uit kan Opus 5 incidenteel `<thinking>`-tags in zichtbare tekst lekken; voor ESS-03 fail-closed (`malformed_response`), voor generatie zou dat in de tekst belanden — alleen met echte calls waarneembaar. Geen `effort` meegestuurd → default `high` → `disabled` toegestaan. | 1008 tests groen incl. `test_ai_clients.py`, `test_ai_service_v2_routing.py`, temperature-guard (22). | opmerking: in de rookproef (en de eerste generatie op Opus 5) op tag-lekkage letten; geen codeactie nu. |
| **D4** | opmerking | `ess03_assessment_service.py:661-676` | Alleen `stop_reason == "max_tokens"` krijgt een eigen foutcode; `refusal` (lege tekst) blijft `malformed_response`, maar `attribution.stop_reason = "refusal"` staat in het foutdocument (zelf geprobeerd) — diagnosticeerbaar, fail-closed. | eigen probe. | opmerking — geen actie. |

Geen blokkerende of "moet vóór commit"-bevindingen.

## Controlepunten (1)–(7)

1. **Thinking-guard** — correct. Families uit config (`opus-5`, `sonnet-5`), `disabled` voor `claude-opus-5`/`claude-sonnet-5`/case-varianten; `anthropic.omit` voor Opus 4.8/4.7/4.6, Haiku, Fable, Mythos en de substring-collisie `claude-opus-50` (`model`/`max_tokens`/`messages` onaangeroerd); lege/malformed policy → omit; echte config-reload via `ConfigManager.reload_configuration()` wordt door dezelfde client gevolgd (`_router` leest per aanroep, DEF-731-patroon behouden); temperature-logica ongewijzigd (gedelegeerd naar `_model_in_capability`; geen externe callers van het verwijderde `_temperature_families`). Omit-pad is hetzelfde SDK-mechanisme als de bestaande temperature-guard → byte-identiek voor niet-gelijste modellen.
2. **`stop_reason`-transport** — correct. `response_hook` wordt in `AsyncGPTClient.chat_completion` gepopt vóór de cachesleutel (`async_api.py:154`), als benoemd argument door `_make_request_with_retries` verwerkt en **niet** aan de providerclient doorgegeven (test: `provider.calls[0]["kw"] == {}` resp. `{"max_retries": 0}`); alleen aangeroepen bij een geslaagde poging. `transport` is lokaal per `generate_definition`-aanroep (lambda over lokale dict) — gelijktijdige aanroepen vermengen niet (zelf geprobeerd met `asyncio.gather`: `end_turn/assessed` naast `max_tokens/truncated_response`). Retourtypen: `AsyncGPTClient.chat_completion` blijft `str`, `AIGenerationResult` ongewijzigd (extra metadata-sleutel alleen bij melding), `ChatResponse.stop_reason` additief met default `None`; cachetreffers ongewijzigd.
3. **ESS-03-afkapping** — correct. Volgorde deadline (R6) → afkapping → kaal JSON (R2) → structuur (R2) → bewijs (R3). `max_tokens` → `status error`, `error.type truncated_response`, `judgment None`, `raw_response_sha256` gevuld, nooit gecachet (tweede aanroep gaat opnieuw naar de provider), **ook bij toevallig parseerbare tekst** (geparametriseerde test). `attribution.stop_reason` alleen aanwezig wanneer gemeld (zelf geprobeerd: `max_tokens` → aanwezig; `end_turn` → aanwezig; geen melding → sleutel afwezig). Provider zonder veld: afgekapte JSON blijft `malformed_response` (regressiewachter).
4. **Prompt-/normhash** — zelf herberekend op de werkboom: `system_prompt_sha256 = e51d06cb3c87…b125c758`, `norm_sha256 = 372bb329fa66…41e028`, `PROMPT_VERSION = ess03-assess/2` — identiek aan HEAD en run v2; `ESS-03.json` en de promptopbouw zijn niet in de diff.
5. **Coördinator-mypy-fix** — gedragsneutraal: `metadata` wordt op regel 1310–1320 altijd als dict-comprehension geconstrueerd; de `None`-guard is dood pad, alleen voor het `dict | None`-veldtype.
6. **Gates** — plausibel voor de huidige werkboom. Manifest v3 = werkboom (10/10 hashes gelijk); `c3-eind-make-test.log:16694,16696`: 6902 passed (6870 + 32), run_profile exit 0; `c3-eind-make-lint.log`, `c3-eind-markers.log`, `c3-eind-complexity.log` (201 → 199) exit 0; `c3-eind-mypy.log` exit 2 op de pre-existing UI-fout; na de coördinatorguard `c3b-mypy.log` "OK: mypy at baseline (0)", `c3b-lint.log` schoon, `c3b-ui-tests.log` 41 passed. De enige wijziging ná de volledige `make test` is de neutrale 4-regelguard; mijn eigen run dekt de editor-suites. Definitieve bevestiging: CI na push.
7. **Regressierisico andere AI-routes** — zie D3: beperkt tot Opus 5 (alle routes), functioneel de intentie (denken uit zoals op 4.8), geen wijziging voor andere modellen; OpenAI-client onaangeroerd.

## Zelf uitgevoerd
- `pytest` drie nieuwe testbestanden → **32 passed**; `tests/unit/services/ai` + alle 21 `test_def766_*` + golden/runtime-matrix/model_router/pricing/config/ai_clients/ai_service_v2_routing/editor-DEF-743/orchestrator/story-2.4 → **1008 passed** (12,3 s).
- Hashvergelijking werkboom ↔ `correction-manifest-v3.json` (10/10 gelijk); prompt-/normhash herberekend; Python-probes voor `attribution.stop_reason`, `refusal`-pad en gelijktijdigheid; grep op hernoemde methode en `max_tokens` per route; logs `c3-eind-*`/`c3b-*` gelezen (exitcodes en samenvattingsregels).
- **Overgenomen:** de documentatiefeiten over Opus 5/Fable/Mythos (coördinator + skill-referentie); `make test`/`make lint`/complexity/markers niet zelf herdraaid.

## Status F1 en F9
- **F1 — gesloten als codecorrectie.** Denken expliciet uit op Opus 5 (1.200 tokens weer tekstbudget zoals op 4.8), afkapping onderscheidbaar (`truncated_response` + `attribution.stop_reason`) en fail-closed, prompt/norm ongewijzigd. Wat nog open staat is precies de meting waarvoor de rookproef bedoeld is: het werkelijke antwoordgedrag van Opus 5 (antwoordvorm, `stop_reason=end_turn`, geen tag-lekkage).
- **F9 — gesloten voor de werkboomstaat** (gates op manifest-v3-staat + c3b-controles na de neutrale guard; mypy nu 0); definitief bevestigd door CI op de gecommitte HEAD.

## Advies
**JA** — gereed om te committen en de rookproef van 2 calls op `claude-opus-5` te draaien. Let daarbij op `attribution.stop_reason` (verwacht `end_turn`), `attempts_observed=1`, en op eventuele `<thinking>`-tags of niet-kale JSON in `raw`-antwoorden (zou `malformed_response` geven). Nieuw seal na commit met `claude-opus-5` als modelbinding; D1 als waiver vastleggen.

*Bronnen: `git diff` werkboom t.o.v. `0ec86aced`; `reports/DEF-766-AI-20260921/uitvoerder/correction-report-v3.md`; `/tmp/def766-ai-20260921/{correction-manifest-v3.json,c3-eind-make-test.log,c3-eind-make-lint.log,c3-eind-complexity.log,c3-eind-markers.log,c3-eind-mypy.log,c3b-mypy.log,c3b-lint.log,c3b-ui-tests.log}`; eigen pytest- en Python-uitvoer hierboven.*