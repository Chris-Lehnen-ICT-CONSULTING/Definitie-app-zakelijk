Jij bent de Claude Code CLI-uitvoerder voor DEF-766 (ESS-03 AI-beoordeling), correctieronde 3, bevinding F1 uit de onafhankelijke delta-review. Voer deze opdracht zelf uit; start geen agents, subagents of extra CLI-sessies. Gebruik de skills test-driven-development en verification-before-completion (lees ze als ze beschikbaar zijn). Doe GEEN echte modelcalls (geen API-aanroepen naar Anthropic/OpenAI; netwerkgrens altijd met fakes). Maak geen commits, geen push, geen PR; verwijder geen bestanden. Antwoord in het Nederlands.

## Context

Werkboom: /Users/chrislehnen/.codex/worktrees/2075/Definitie-app (je cwd), branch feature/DEF-766-ess03-ai-beoordeling, HEAD 0ec86aced (schoon). Lees eerst:
- reports/DEF-766-AI-20260921/onafhankelijk/review-delta-v1.md — vooral bevinding F1 en § 4 "Modelwissel".
- docs/plans/2026-09-21-DEF-766-ess03-ai-beoordeling-v1.md — acceptatiecontract punt 1, 3 en 6 (technisch defect = error, nooit pass; geen stil herstel; begrensde uitvoer).
- src/services/ai/anthropic_client.py (temperature-guard DEF-441/731 als patroon), src/services/ai/base_client.py (ChatResponse), src/services/ai/model_router.py (accepts_temperature + config-capabilities), config/config.yaml (model_routing.capabilities), src/services/validation/ess03_assessment_service.py (_beoordeel_met_model / _beoordeel_antwoord, max_tokens=1200), tests/unit/services/ai/test_anthropic_client_temperature_guard.py.

## Het probleem (geverifieerd in de Anthropic-documentatie op 21-09-2026, platform.claude.com/docs/en/build-with-claude/thinking)

De app-default is sinds commit 0ec86aced `claude-opus-5`. Op Claude Opus 5 (en Sonnet 5, Fable 5, Mythos 5) staat adaptive thinking standaard AAN wanneer de `thinking`-parameter wordt weggelaten; op Opus 4.8/4.7/4.6 staat het standaard uit. Denktokens tellen mee in `max_tokens`. De app gebruikt kleine budgetten (300 default, 500 AIServiceV2, 800 synoniemen, 1200 ESS-03). Gevolg: afgekapte of lege antwoorden. Thinking kan expliciet uit met `thinking={"type": "disabled"}` (toegestaan bij effort high of lager; de app stuurt geen effort). Bij afkapping geeft de API `stop_reason: "max_tokens"`; de client leest `stop_reason` nu niet.

## Opdracht (beperkt, twee delen)

### Deel 1 — thinking expliciet uit, configuratiegestuurd
Volg het bestaande patroon van de temperature-guard: een capability-lijst in `config/config.yaml` onder `model_routing.capabilities.anthropic`, bijv. `thinking_default_on: { source: "https://platform.claude.com/docs/en/build-with-claude/thinking", checked_at: "2026-09-21", model_families: ["opus-5", "sonnet-5", "fable-5", "mythos-5"] }`, en een `ModelRouter`-methode (bijv. `thinking_default_on(model, provider=...)`) met dezelfde substring/case-insensitive-matching als `accepts_temperature`. In `AnthropicClient` stuurt `messages.create` voor zo'n model `thinking={"type": "disabled"}` mee; voor andere modellen wordt de parameter weggelaten (`anthropic.omit`), zodat het gedrag voor Opus 4.8 e.d. byte-identiek blijft. Log op debug-niveau wat er gebeurt. Geen effort-parameter toevoegen. Geen wijziging aan OpenAIClient behalve als de abstractie het vereist (dan alleen een no-op).

### Deel 2 — stop_reason zichtbaar en afkapping fail-closed in ESS-03
`ChatResponse` (base_client.py) krijgt een optioneel veld `stop_reason: str | None` (additief; bestaande constructies blijven werken). `AnthropicClient` vult het uit `response.stop_reason`; bij `"max_tokens"` een waarschuwing loggen (zonder sleutels of promptinhoud). Zoek uit hoe `stop_reason` door `AIServiceV2.generate_definition`/de bestaande responsobjecten heen kan reizen naar `Ess03AssessmentService` zonder de bestaande publieke retourvorm te breken (kies de kleinste additieve route en rapporteer die). In `Ess03AssessmentService`: is `stop_reason == "max_tokens"`, dan status `error` met een eigen foutcode (bijv. `truncated_response`), géén oordeel, niet gecachet, en de reden benoemt de afkapping — onderscheidbaar van `malformed_response`. Als het transport van stop_reason naar de dienst een grotere wijziging vergt dan ~40 regels, stop dan bij deel 1 + het ChatResponse-veld + de waarschuwing, en rapporteer expliciet wat je hebt weggelaten en waarom.

### Werkwijze
- TDD: eerst falende tests (RED, exitcode vastleggen in een log), dan minimale implementatie, dan GREEN. Tests naast de bestaande: `tests/unit/services/ai/test_anthropic_client_thinking_guard.py` (thinking disabled voor opus-5/sonnet-5/fable-5; weggelaten voor opus-4-8/opus-4-6; config-reload volgt; substring-collisie), en voor deel 2 een test in `tests/unit/validation/test_def766_ess03_correcties_service.py` of een nieuw `test_def766_ess03_truncation.py` (fake provider met stop_reason max_tokens → error/truncated_response, judgment None, cache leeg).
- Gerichte tests met `/Users/chrislehnen/Projecten/Definitie-app/.venv/bin/python -m pytest <paden> -q -p no:cacheprovider -o addopts=""`. Daarna alle `tests/unit/services/ai`, alle `test_def766_*`, `test_model_router.py`, `test_config_system.py`, `test_ai_clients.py`, `test_ai_service_v2_routing.py`.
- Daarna de volledige gates op de eindstand (dit is ook bevinding F9): `make test` met `PY=/Users/chrislehnen/Projecten/Definitie-app/.venv/bin/python` (of zoals de Makefile het verwacht — inspecteer de Makefile), `make lint`, `make complexity-check`, `make test-markers-check`. Logs naar /tmp/def766-ai-20260921/c3-*.log met exitcodes.
- Geen `print()` in productiecode; ruff + black schoon; geen TODO-markers; geen letterlijke tekst `asyncio.run(` in src/services (hook).
- Blijf binnen scope: geen andere refactors, geen prompt-/normwijziging (prompt `ess03-assess/2` en de norm moeten hash-identiek blijven), geen schema-/dependencywijziging.

## Rapport
Schrijf /tmp/def766-ai-20260921/correction-report-v3.md met: wat gewijzigd (bestand:regel), de gekozen transportroute voor stop_reason, RED/GREEN-bewijs met logbestanden en exitcodes, uitkomst van alle gates (aantallen, exitcodes), bevestiging dat systeemprompt-hash en normhash ongewijzigd zijn (bereken ze zoals de runner `scripts/ess03/run_ess03_gevallen.py` dat doet), en resterende beperkingen. Geef dat rapport ook volledig als eindantwoord. Stop daarna.
