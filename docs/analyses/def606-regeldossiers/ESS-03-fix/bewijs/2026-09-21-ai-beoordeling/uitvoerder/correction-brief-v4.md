Jij bent de Claude Code CLI-uitvoerder voor DEF-766 (ESS-03 AI-beoordeling), correctieronde 4: twee CI-fouten op PR #467. Voer deze opdracht zelf uit; start geen agents, subagents of extra CLI-sessies. Geen echte modelcalls (netwerkgrens met fakes). Geen commits, push of PR; niets verwijderen. Antwoord in het Nederlands.

Werkboom /Users/chrislehnen/.codex/worktrees/2075/Definitie-app (je cwd), branch feature/DEF-766-ess03-ai-beoordeling, HEAD c0d3423ab (schoon). PR #467 naar main; CI-runs: integration-tests, tests (acceptance/smoke) en "Run Preflight Checks" falen; de unitgate, lint, mypy, complexity, gitleaks, semgrep, pip-audit e.d. zijn groen. Lokaal `make test` (unit) is groen; deze twee fouten zitten buiten de unitgate.

## Fout 1 — offline kernjourney: ESS-03 geeft `error` door een testfake zonder `max_retries`

Log (integration én acceptance): `ERROR utils.async_api:async_api.py:212 API call failed: BevrorenAIClient.chat_completion() got an unexpected keyword argument 'max_retries'` → `ESS-03: telbaarheidsbeoordeling mislukt (connection)` → `tests/integration/test_offline_core_journey.py::test_offline_kernjourney_van_generatie_tot_export` faalt op `VERWACHTE_DEKKING` (`error: 1` i.p.v. 0, `review_required: 15` i.p.v. 16).

Oorzaak: `Ess03AssessmentService` zet de opt-ins `max_attempts=1`/`max_retries=0`; `AsyncGPTClient._make_request_with_retries` geeft `max_retries` door aan de providerclient (`src/utils/async_api.py` ~regel 245–260). De echte `AnthropicClient`/`OpenAIClient` accepteren dat; de bevroren testclients niet: `tests/integration/functionality/conftest.py:150` `BevrorenAIClient.chat_completion(messages, model, temperature, max_tokens, timeout)` en `tests/integration/test_synonym_orchestrator_e2e.py:90` (zelfde signatuur).

Opdracht:
1. Onderzoek eerst welke fake de kernjourney gebruikt (volg de fixture-keten van `test_offline_core_journey.py`) en of de `AsyncAIClient`-abstractie in `src/services/ai/base_client.py` `max_retries` als optioneel keyword documenteert. Kies de kleinste correcte fix. Voorkeur: de fakes volgen de providerinterface (`max_retries: int | None = None` als optioneel keyword, ongebruikt), NIET de productiecode versoepelen door kwargs stil weg te laten — tenzij je aantoont dat de abstractie het keyword niet voorschrijft; rapporteer je keuze.
2. Daarna beoordeelt ESS-03 in de journey het bevroren antwoord. Bepaal wat de fake op de ESS-03-prompt teruggeeft (herken de prompt: hij bevat het ESS-03-normblok en vraagt om een JSON met `verdict`/`applicability`/`unit`/`reason`/`evidence`/`missing_information`/`question`/`uncertainty`; zie `bouw_beoordelingsprompt` en `parse_modeluitvoer` in `src/services/validation/ess03_assessment_service.py` en `src/domain/ess03/contract.py`). Laat de fake voor ESS-03 een geldig, gesloten antwoord geven met `verdict: insufficient_information`, precies één gerichte vraag, en minstens één citaat dat letterlijk in het verzonden materiaal staat (bijv. de definitieregel uit de prompt, of de begripsnaam; controleer met de citaatcontrole van de dienst). `insufficient_information` telt in de dekking als `review_required`, zodat `VERWACHTE_DEKKING` (33/29/4/16/4/0) ongewijzigd blijft en de DEF-766-toelichting boven `VERWACHTE_DEKKING` alleen tekstueel wordt bijgewerkt (AI-beoordeling i.p.v. "menselijk oordeel"; bevroren antwoord = onvoldoende informatie). Als de dienst `error` of `malformed_response` blijft geven, los dat op in de fake, niet in de dienst. Verander geen productieprompt/-norm (hashes moeten identiek blijven: systeemprompt `e51d06cb…`, norm `372bb329…`).
3. Doe hetzelfde voor de tweede `BevrorenAIClient` (synonym e2e) voor zover die door ESS-03 wordt geraakt; anders alleen de signatuur.

## Fout 2 — DEF-563 timing-inventaris (Preflight, blokkerend)

`python -I -B scripts/ci/timing_assert_ratchet.py .` meldt 5 nieuwe kandidaten en exit 1:
- tests/unit/services/test_def766_ai_route_optins.py::test_heuristische_tokenraming_slaat_tiktoken_over
- tests/unit/validation/test_def766_ess03_assessment_service.py::test_gegronde_beoordeling_via_taakrouting_zonder_hardcoded_model
- tests/unit/validation/test_def766_ess03_correcties_service.py::test_trage_aanroep_wordt_door_de_deadline_afgebroken
- tests/unit/validation/test_def766_ess03_correcties_service.py::test_vangnet_een_te_laat_teruggekeerd_antwoord_is_timeout_zonder_oordeel
- tests/unit/validation/test_def766_ess03_correcties_service.py::test_vertraagde_nabewerking_op_de_echte_route_komt_binnen_de_deadline_terug

Lees `scripts/ci/timing_assert_ratchet.py` (er is bewust geen `--update`), `docs/testing/def563-timing-baseline.json` (schema met `entries`: `source_sha256`, `comparisons`, `status` ∈ {unreviewed, retain-risk, deterministic, contract, non-timing}, `reason`) en `docs/testing/def563-timing-inventaris.md`. Voeg de 5 kandidaten met een expliciet beoordeelde status en een inhoudelijke reden toe aan de baseline (gebruik `--inventory` om de exacte sleutels/hashes/comparisons te genereren; beoordeel elke test: welke vergelijking, of hij wall-clock-gevoelig is (bijv. `elapsed < 0.45` bij een 0,6 s-blokkade → `retain-risk` met motivering; een tokenraming-vergelijking zonder klok → `non-timing`/`deterministic`)). Werk `def563-timing-inventaris.md` bij met een korte paragraaf "DEF-766 — 22 september 2026". Voer daarna `python -I -B scripts/ci/timing_assert_ratchet.py .` uit → exit 0. Let op: als je in fout 1 een testbestand met een timingvergelijking wijzigt, verandert zijn `source_sha256` — draai de inventaris als laatste.

## Gates
Gerichte tests met `/Users/chrislehnen/Projecten/Definitie-app/.venv/bin/python -m pytest <pad> -q -p no:cacheprovider -o addopts=""`. Daarna, met `PY=/Users/chrislehnen/Projecten/Definitie-app/.venv/bin/python`: `make test-integration`, `make test-acceptance` (inspecteer de Makefile voor de exacte targets die CI gebruikt: `.github/workflows/*.yml`), `make test` (unit), `make lint`, `make mypy-check`, `make complexity-check`, `make test-markers-check`, én de preflight-scripts uit de workflow (timing ratchet e.a.). Logs naar /tmp/def766-ai-20260921/c4-*.log met exitcodes. Als de integration-suite lokaal hangt op een real-API-test (bekend risico DEF-428/429), meld dat met de testnaam en draai de rest gericht.

## Rapport
`/tmp/def766-ai-20260921/correction-report-v4.md`: wijzigingen (bestand:regel), keuze bij fout 1 met motivering, per timingkandidaat status + reden, gate-uitkomsten met exitcodes, bevestiging prompt-/normhash ongewijzigd (zoals `logs/def766-c3/hashes.py`), resterende beperkingen. Geef het rapport ook volledig als eindantwoord. Stop daarna.
