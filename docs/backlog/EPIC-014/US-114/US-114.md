# US-114: Implementeer Context Integration Handler

## User Story
**Als** context processor  
**Wil ik** context informatie integreren in definities  
**Zodat** definities contextueel relevant zijn zonder expliciet te verwijzen  

## Status
**Status**: TODO  
**Priority**: LOW  
**Story Points**: 3  
**Epic**: [EPIC-014: Business Logic Refactoring](../EPIC-014.md)  

## ⚠️ BELANGRIJKE NOTITIE
**Domein field is verwijderd in EPIC-010/US-043**. Deze handler moet aangepast worden voor moderne context approach.

## Business Context
Integreert context informatie subtiel in definities zonder expliciete vermelding van organisaties of contexten.

## Acceptance Criteria
- [ ] Context integratie zonder "binnen de context van"
- [ ] Parse context voor relevante delen
- [ ] Alleen toevoegen als context niet al aanwezig
- [ ] Format: impliciet in definitie verwerkt
- [ ] GEEN domein field gebruiken (removed)

## Modern Implementation
```python
def integrate_context(definitie: str, context: str) -> str:
    # Check if context already integrated
    if any(part.lower() in definitie.lower() 
           for part in context.split(',') if len(part) > 2):
        return definitie
    
    # Modern approach: weave context into definition
    # NOT: "... (binnen de context van X)"
    # BUT: natural language integration
    return enhance_with_context(definitie, context)
```

## Migration Notes
- Oude code gebruikt `definition.domein` → VERWIJDERD
- Nieuwe approach: rijke context velden
- Juridische context, organisatorische context arrays

## Dependencies
- Modern context model (post EPIC-010)
- NLP utilities voor natural integration

## Priority
LOW - Context wordt nu beter afgehandeld door V2 prompts