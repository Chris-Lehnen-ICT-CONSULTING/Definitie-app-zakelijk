# US-443: Remove Monitoring Tab

**Epic:** EPIC-028 - Feature Cleanup & UI Simplification
**Priority:** HIGH
**Effort:** 2 story points
**Status:** 🔵 TODO

---

## 📖 User Story

**Als** eindgebruiker
**Wil ik** geen developer/ops tools in de UI zien
**Zodat** ik focus kan houden op definitie generatie en review

---

## 🎯 Acceptance Criteria

- [ ] `src/ui/components/monitoring_tab.py` verwijderd (400 LOC)
- [ ] Tab registration verwijderd uit `tabbed_interface.py`
- [ ] Import statements verwijderd
- [ ] Tab verschijnt niet meer in UI (was 8 tabs → nu 6 tabs)
- [ ] Monitoring functionaliteit verplaatst naar external tools (logs, APM)
- [ ] Tests blijven groen

---

## 🔧 Technical Tasks

### 1. Remove Files
```bash
rm src/ui/components/monitoring_tab.py
```

### 2. Update `src/ui/tabbed_interface.py`

**Remove import (line ~59):**
```python
from ui.components.monitoring_tab import MonitoringTab
```

**Remove initialization (line ~190):**
```python
self.monitoring_tab = MonitoringTab(self.repository)
```

**Remove tab config (lines ~236-240):**
```python
"monitoring": {
    "title": "📈 Monitoring",
    "icon": "📈",
    "description": "Performance monitoring en API cost tracking",
},
```

**Remove render call (lines ~1664-1665):**
```python
elif tab_key == "monitoring":
    self.monitoring_tab.render()
```

### 3. Alternative Monitoring (Documentation)

Create note for ops/developers:

```markdown
# Monitoring Vervangen

De Monitoring Tab is verwijderd uit de UI. Gebruik deze alternatieven:

## API Costs
- Check `logs/api_calls.json` voor cost tracking
- Gebruik `CostCalculator` service direct in scripts

## Performance
- Check application logs in `logs/`
- Gebruik external APM tools (Datadog, New Relic, etc.)

## System Health
- Database stats via `repository.get_statistics()`
- Service health via health check endpoints
```

### 4. Scan for References
```bash
grep -r "monitoring_tab\|MonitoringTab" src/ --include="*.py"
```

### 5. Run Tests
```bash
pytest tests/ -v
```

---

## 📋 Testing Checklist

- [ ] UI loads zonder errors
- [ ] Tab menu toont 6 tabs (geen Monitoring meer)
- [ ] Geen import errors
- [ ] Definition generation werkt nog steeds
- [ ] All existing tests pass
- [ ] No broken references

---

## 📊 Impact

**Before:** 8 tabs (incl. Monitoring)
**After:** 6 tabs

**LOC Removed:** ~400
**Files Removed:** 1

---

## 🔗 Dependencies

- **After:** US-441, US-442

---

## 📝 Notes

- Monitoring Tab was developer tool, niet voor eindgebruikers
- Cost tracking blijft beschikbaar via logs
- Performance metrics via external tools is standaard practice
