# US-441: Remove Orchestration Tab

**Epic:** EPIC-028 - Feature Cleanup & UI Simplification
**Priority:** HIGH
**Effort:** 2 story points
**Status:** 🔵 TODO

---

## 📖 User Story

**Als** developer
**Wil ik** de deprecated Orchestration Tab verwijderen
**Zodat** de codebase cleaner is en gebruikers niet verward worden door legacy features

---

## 🎯 Acceptance Criteria

- [ ] `src/ui/components/orchestration_tab.py` verwijderd (500 LOC)
- [ ] Tab registration verwijderd uit `tabbed_interface.py`
- [ ] Import statements verwijderd
- [ ] `ENABLE_LEGACY_TAB` feature flag verwijderd uit `config/feature_flags.py`
- [ ] Tab verschijnt niet meer in UI
- [ ] Geen broken imports in codebase
- [ ] Tests blijven groen

---

## 🔧 Technical Tasks

### 1. Remove Files
```bash
rm src/ui/components/orchestration_tab.py
```

### 2. Update `src/ui/tabbed_interface.py`

**Remove:**
- Lines 64-67: Import statement (commented out)
- Lines 192-200: Orchestration tab initialization
- Lines 246-251: Tab config entry (commented out)
- Lines 1596-1605: Tab registration check
- Lines 1669-1673: Render method call

**Code to remove:**
```python
# Lines 192-200
self.orchestration_tab = None
if FeatureFlags.ENABLE_LEGACY_TAB.is_enabled():
    try:
        from ui.components.orchestration_tab import OrchestrationTab
        self.orchestration_tab = OrchestrationTab(self.repository)
    except ImportError:
        st.warning("OrchestrationTab module not available")

# Lines 1596-1605
if FeatureFlags.ENABLE_LEGACY_TAB.is_enabled() and self.orchestration_tab:
    export_index = (
        tab_keys.index("export") if "export" in tab_keys else len(tab_keys)
    )
    tab_keys.insert(export_index + 1, "orchestration")
    self.tab_config["orchestration"] = {
        "title": "🎭 Orchestration (Legacy)",
        "icon": "🎭",
        "description": "Legacy orchestratie - deprecated",
    }

# Lines 1669-1673
elif tab_key == "orchestration":
    if self.orchestration_tab:
        self.orchestration_tab.render()
    else:
        st.info("Legacy orchestration tab is not available.")
```

### 3. Update `src/config/feature_flags.py`

**Remove:**
```python
ENABLE_LEGACY_TAB = FeatureFlag(
    name="Enable Legacy Orchestration Tab",
    description="Schakel legacy orchestration tab in (deprecated)",
    category=FeatureFlagCategory.LEGACY,
    default_value=False,
    env_var="ENABLE_LEGACY_TAB",
)
```

### 4. Scan for other references
```bash
grep -r "orchestration_tab\|OrchestrationTab\|ENABLE_LEGACY_TAB" src/ --include="*.py"
```

### 5. Run tests
```bash
pytest tests/ -v
```

---

## 📋 Testing Checklist

- [ ] UI loads zonder errors
- [ ] Tab menu toont alleen 7 tabs (niet 8)
- [ ] Geen import errors in logs
- [ ] Feature flag is weg uit config
- [ ] All existing tests pass
- [ ] No broken references in codebase

---

## 📊 Impact

**Before:** 8 tabs (incl. Orchestration deprecated)
**After:** 7 tabs

**LOC Removed:** ~500
**Files Removed:** 1

---

## 🔗 Dependencies

None (kan als eerste uitgevoerd worden)

---

## 📝 Notes

- Tab was al feature-flagged (disabled by default)
- Geen gebruikers zullen dit missen
- V2 orchestrator doet deze functionaliteit al
