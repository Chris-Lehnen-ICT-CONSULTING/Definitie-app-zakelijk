---
id: BUG-001
titel: Export functionaliteit niet zichtbaar in Quality Control tab
status: Resolved
prioriteit: Medium
aangemaakt: 2025-09-29
bijgewerkt: 2025-09-29
opgelost: 2025-09-29
epic: EPIC-023
story: US-424
component: quality_control_tab
severity: Minor
type: bug
tags:
  - ui
  - export
  - quality-control
canonical: true
---

# BUG-001: Export functionaliteit niet zichtbaar in Quality Control tab

## Beschrijving
De export functionaliteit voor Quality Control rapporten is volledig geïmplementeerd in `quality_control_tab.py` (regels 359-419) maar wordt niet weergegeven in de UI omdat de methode `_render_export_options()` niet wordt aangeroepen vanuit de `render()` methode.

## Probleem
- **Locatie**: `src/ui/components/quality_control_tab.py`
- **Methode**: `_render_export_options()` op regel 359-419
- **Issue**: Methode wordt niet aangeroepen in `render()` methode

## Impact
Gebruikers kunnen de volgende functionaliteit niet bereiken:
1. Export Analyse Rapport button
2. Export Validatie Rapport button
3. Reset Analyse Cache button

Deze functionaliteit is volledig geïmplementeerd maar onbereikbaar.

## Huidige Code Structuur
```python
def render(self):
    """Render quality control tab."""
    # ... existing sections
    self._render_toetsregels_analysis()
    self._render_system_health()
    self._render_validation_consistency()
    self._render_rule_coverage_analysis()
    # MISSING: self._render_export_options()
```

## Oplossing
Voeg de volgende regel toe aan de `render()` methode na regel 48:

```python
st.markdown("---")
self._render_export_options()
```

## Verificatie Stappen
1. Open Quality Control tab
2. Scroll naar beneden
3. Verifieer dat Export sectie zichtbaar is met 3 buttons:
   - 📊 Export Analyse Rapport
   - ✅ Export Validatie Rapport
   - 🔧 Reset Analyse Cache
4. Test elke button om te verifiëren dat export werkt

## Alternatief
Als de export functionaliteit bewust is uitgeschakeld:
1. Documenteer de reden in de code
2. Overweeg de ongebruikte code te verwijderen
3. Update US-424 status naar "Won't Do" met uitleg

## Notes
- De geïmplementeerde code lijkt volledig functioneel
- Export schrijft naar `exports/` directory
- Gebruikt timestamp in bestandsnamen

## Resolutie
**Opgelost op**: 2025-09-29

**Fix toegepast**:
- Toegevoegd `self._render_export_options()` aan `render()` methode op regel 51
- Export sectie is nu zichtbaar in de Quality Control tab UI
- Alle drie export buttons zijn nu bereikbaar voor gebruikers

**Verificatie**:
- Python syntax check: ✅ Geslaagd
- Import test: ✅ Geslaagd
- Code compileert zonder fouten