Backlogitem – Temperatuurparameter instelbaar via settings/config

🆔 ID: GPT-Temp-Config
📅 Datum: 2025-07-04
🎯 Doel: De temperatuurparameter voor ChatGPT-API centraal beheren via een settings- of configbestand
🔍 Probleem: Temperatuurinstelling staat nu hardcoded in de codebase, wat beheer en aanpassing lastig en foutgevoelig maakt
📈 Impact: Flexibeler beheer, eenvoudig aanpasbare AI-output, minder risico op fouten en snellere iteraties
✅ Acceptatiecriteria:
* Temperatuurparameter is uit de code gehaald en opgenomen in een settings- of configbestand (bijv. `config.py`, `.env`, of `settings.json`)
* Alle relevante API-calls lezen de temperatuurparameter uit deze centrale instelling
* Aanpassen van de temperatuur kan zonder codewijziging, alleen via settings/config
* Instelling is duidelijk gedocumenteerd (README of interne docu)
* Er is fallback (defaultwaarde) als de parameter ontbreekt

📦 Bestand: bijv. `config.py`, `.env`, `settings.py`, en alle bestanden waar de ChatGPT-call plaatsvindt
👥 Betrokkenen: Ontwikkelaar
⏱️ Prioriteit: Snel oppakken
🗓️ Sprint: 1
Status: To do

---

1️⃣ Analyse bestaande situatie

* De temperatuurparameter wordt direct in de Python-code opgegeven of als default-waarde gebruikt bij het aanroepen van de ChatGPT-API.
* Aanpassen van deze waarde vereist aanpassing van broncode, wat foutgevoelig en niet schaalbaar is.

---

2️⃣ Wat moet worden aangepast en waarom

* Verplaats de temperatuurinstelling naar een centraal settings/configbestand.
* Hierdoor kunnen ontwikkelaars en beheerders eenvoudig de AI-uitvoer tunen zonder de code aan te passen.
* Draagt bij aan betere DevOps, versiebeheer en consistent beheer.

---

3️⃣ Oplossingsrichting

* Voeg de parameter toe aan het settings/configbestand, bijvoorbeeld:
  - `.env`:
    GPT_TEMPERATURE=0.3
  - of `config.py`:
    GPT_TEMPERATURE = 0.3
* Lees deze waarde uit in de Python-code:
    import os

    temperature = float(os.getenv('GPT_TEMPERATURE', 0.3))
    response = openai.ChatCompletion.create(
        model="gpt-4",
        messages=messages,
        temperature=temperature,
        ...
    )

---

4️⃣ Validatie / zelfcontrole

* Wijzigen van de temperatuur in het configbestand heeft direct effect op de output, zonder codewijziging.
* Code en documentatie zijn gecontroleerd; defaultwaarde werkt bij ontbreken van parameter.
* Instelling is zichtbaar in documentatie/README.

---

🔁 Kwaliteitsopmerkingen conform werkafspraak

* Instelling is gecentraliseerd, versiebeheerbaar en traceerbaar.
* Geen hardcoded waarden meer in de code.
* Wijziging is eenvoudig te testen en terug te draaien.
* Werkafspraak toegepast: duidelijke structuur, validatie, beheerbaarheid en hergebruik.
